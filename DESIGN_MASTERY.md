# 熟練度改版規格(DESIGN_MASTERY)

> 狀態:**已和使用者敲定、尚未實作**(2026-06-27)。這是「學習引擎核心」的大改方向。
> 實作前先讀這份 + `HANDOFF.md`。會把現行「計次(`NEED_RUNG`)」整套換掉。

## 為什麼改
- 現行熟練度是「每階(認/說/寫)答對 `NEED_RUNG=2` 次就過」的**計次**。使用者不認同:覺得「2 次就會」太隨便,也不是他要的東西。
- 使用者要的是**連續累積 %**:答對 +%、答錯 −%、累積到 100% = 學會。
- 另一個動機:Duolingo 那種「答錯就把同一題再砸給你、不給梯子」會把人卡死(使用者卡在寫句子那關)。要用「答錯 → 回顧 → 再答」解掉。

## 核心模型

### 1. 熟練度 = 連續 %(取代計次)
- 每個單位一條 `mastery`,0~100,**100 = 學會**。
- **答對 +25、答錯 −20**(預設,可調)。
- **答錯只扣 %,沒別的處罰**(練習模式不掉命;命只在王戰)。
- 第一次「教」**不加不扣**(只解鎖後面題型)。
- 學會(100%)後若再答錯掉破 100 → **回到這關再練**(use-it-or-lose-it)。
- **說題被機器聽錯**:保留現行寬鬆比對 / 自評,**不因辨識誤判扣分**。

### 2. 兩條熟練度軌
- **單字 %**(每個字一條)= 認得 / 會用這個字。**一個字一條總 %**,不分認/說/寫(那些只是題型)。
- **句型 %**(每個 `PATTERNS` 句型一條)= 會不會排這個結構的「排序邏輯」。

### 3. 出哪種題:關卡解鎖 × 熟練度頻率(兩軸並行,2026-06-27 定案)
- **關卡 = 解鎖進度**:每個題型格式標 `lv`(第幾關解鎖)。一關的題型池 = 所有 `lv ≤ 當前關` 的格式(**向下取 / 累加**)→ 關卡越高越豐富、同一關混多種。新字一律先教。解鎖表見 `index.html` 的 `FORMATS`(L1 選/配/跟讀說 → … → L5 默寫)。
- **熟練度 = 出現頻率(不是 gate)**:`tierOfMastery` 取目標難度,池子裡靠近的題型權重高、遠的低 → 難題不消失只變少;字越熟、難題越多;剛學的以簡單為主。
- ⚠ 早期誤區(已修正):一度做成「關卡**取代**熟練度」單軸 → 錯。是**兩軸並行**:關卡決定有哪些、熟練決定哪種多。
- **句型軌**(看句型 %):低 → **排詞**;中 → **填空**;高 → **整句默寫**(最難、量少)。句型題要「組成的字夠熟 + `requires` 功能詞已教」才會出。(待 step 4/5 接上。)

### 4. 錯點歸因(句子題)
- 答錯 → 抓出錯的點,**只扣 / 只回顧那個點**,不連坐:
  - **用錯字 / 拼錯字** → 扣**那個字**的 %。
  - **字都對、只是順序錯** → 扣**那個句型**的 %(不冤枉單字)。
- 答對 → 相關字 + 句型都加。
- 可歸因程度看題型:**排詞 / 填空** 哪格錯一目了然(乾淨);**整句默寫** 用比對抓拼錯 / 用錯字,純文法 / 順序錯歸到句型、難精準到字。

### 5. 答錯 → 回顧 → 重答(關卡流程)
- 一關 = 固定 N 道題(**主回合**)。
- 答錯的題收集;主回合跑完 → **回顧補練回合**:先重看一次(念 + 中文 + 字根 / 句型)→ 再答一次。答對才消;又錯再回顧,**永不卡死**。
- **上方進度條** = 答了幾題 / 共幾題(主回合 + 補練都算)。逐題進度已先做(見 CHANGELOG)。

## 數字(預設,之後憑手感調)
| 項 | 值 |
|---|---|
| 答對 | +25% |
| 答錯 | −20% |
| 教 | 不加不扣 |
| 學會門檻 | 100% |

## 現況盤點(實作前先知道)
- **已有、要接進系統**:`askBuildSentence`(= 排詞)、`askClozePick`(= 填空)現在只是「過關後選配 / 認階」的半成品,沒接熟練度、答錯不歸因。
- **要新做**:整句默寫(free-type 整句)。
- **要拔掉 / 改寫**:`NEED_RUNG` 計次那套 —— `reps[]`、`rungOf`、`pOf`、`isLearned`、`chooseRung`、`quota`/`lgot` 都改吃 `mastery %`。
- **schema 變更**:`store[id]` 從 `{taught, reps[], coined, miss}` → `{taught, mastery, coined}`;新增句型 store(`{patId: mastery}`)。舊資料 migration:用舊 `pOf%` 換算成初始 `mastery`,或直接 reset。

## 實作順序(小步、每步驗)
1. ✅ **單字層計次 → 連續 %**(做完、驗過 2026-06-27):store schema 換 `mastery`、`onCorrect`/`onWrong` 加減 %、`rec()` 自癒 migration、`rungOf`/`isLearned`/`pOf` 改吃 %。
3. ✅ **題型難度改吃單字 %**(隨 step 1 一起做):`tierOfMastery`(<34 認/<67 說/餘 寫)、`levelPlan` topRung 固定 3、`askSpeak`/`askWrite` 鷹架改吃 mastery。
2. ✅ **答錯回顧重答回合**(做完、驗過 2026-06-27):`onWrong`→`reviewQueue`(延後)、`nextQuestion` 三段(主→回顧→done)、`reviewThenAsk` 先重看再重答、回顧答對清題、又錯重新延後永不卡死。quota 改「新字 2(教+考)/ 複習字 1」。
4. **句型 %**(第二條軌)+ 把現有排詞 / 填空接進主流程 + 錯點歸因。
5. **整句默寫**(新題型,金字塔頂)。

## 6. 間隔複習(SRS)+ 浮動關卡(2026-06-27 定案,核心脊椎)
> 解決使用者點破的兩個問題:① 固定 5 字 → 學越多越記不住(複習量不夠);② 句子 / 主題本身需要多字同場(「我叫大衛、我來自中國」隨便破 5)。**關卡大小浮動、由內容決定,沒有固定 5 字。**

### 字的四種狀態
**新(fresh)** 沒教過 → **學習中(active)** 教過 <100%、主力 → **學會(learned)** 100%、進間隔複習 → **卡住(lapsed)** 複習答錯掉破 100% → 退回 active 優先補。

### SRS 排程(每字)
- 記 `due`(下次該複習的 tick)、`ivl`(目前間隔 tick 數)。全域 `meta.clock` 每開一關 +1。
- 剛學會 → `ivl=1`、`due=clock+1`。
- 學會字被複習**答對** → `ivl ×2`(封頂 ~30)、`due=clock+ivl`(越拉越久)。
- **答錯** → `ivl=1`、`due=clock+1`(很快再考)、`mastery −20`(退回 active)。

### `buildLevel` 浮動組成(上限 `MAX≈10`,超出順延下一關)
依優先序填到 MAX:① **新字** `NEW`(2,保證進度)→ ② **active 學習中**(cap ~5,主力)→ ③ **due reviews**(`due ≤ clock`、最逾期先,填滿剩餘)→ ④(句子/主題階段)該主題整組字(step 4)。沒固定 5 字。

### 關卡內容隨階段演化(使用者 2026-06-27 補:後期不能只看字數)
一關的「內容單位」會長大,`buildLevel` 之後要 phase-aware:
- **字階段(早期)**:一關 = 字(新 + 學習中 + 到期複習)。← 目前的浮動關卡。
- **主題階段(中期)**:一關 = 某主題的句子(排詞/填空/默寫)+ 該主題主體字 + 隨機抽的舊字複習。單位變「句子」,字是配料,限制是「一個主題」不是字數。
- **句子階段(後期)**:句子/字都熟 → 拔掉刷字,一關純造句(新舊字組合運用),沒有單字 quota。
- 前提:先做 step 4(句子進主流程)+ 把內容組織成「主題」(句型 + 字打包)。

### 惡補關 + 打王
- **打王入場不擋**(過階段就能打,無 100% 門檻)。
- **輸 → 收集王關拼錯的字 → 組一關只有那些字的惡補關 → 清掉 → 王重新解鎖**(取代入場門檻)。
- 一般 lapsed 字因 `due=clock+1` 本來就下關優先回來。

### 拿掉關卡預覽
- `showStart` 的字表預覽拿掉(固定小關卡的產物;浮動 + 多字後沒意義,原描述「認識→選→說→寫」也過時)。

## 未決(不屬本規格,之後再談)
- **前期節奏 / 早期變化**(option A 後半):使用者回饋「後期好玩、前期無聊」。**已做**:① 跨題不連續同形式(`lastFormat`);② 教完隔 ~2 題就考;③ **錯開新字引入(`buildLevel` NEW_PER_LEVEL=2)→ 關卡混階**(治「第 1~4 關整關同一階」的根因)。**還缺**:有遊戲感題型往前擺、早點有刺激(連擊 / 小挑戰)。
- **題型擴充**:**已做一部分**:看圖說(`renderSpeak` pic 模式)、看圖寫(`askPicType`)。**還可加**:更多新題型 / 新玩法。
- 章節 / 關卡骨架:**已做**:錯開新字引入(`NEW_PER_LEVEL=2`)、學會的字定期抽考(`SPOTCHECK=1`,還在學的=主力)。**還缺**:輕量 SRS 間隔(抽考優先抽最久沒考的,現為隨機)、章節結構 / 多王。
- 「答過的題降權重 / 別重複出題」「複習混合比例」。
- 難度 % 的確切切點(看圖→說→默寫、排詞→填空→默寫 各在百分之幾切)。
- 不變的地基:引擎 / 玩法層分離、分批解鎖 + 王守門、金幣、技能開關 —— 這次只換「熟練度怎麼算 + 答錯怎麼補」,別動到那些。

## 本階段引擎新規則(2026-06-27 起;逐筆見 CHANGELOG,結構面見 CURRICULUM.md「★結構定案」)
> 以下是這波實作 / 敲定的引擎層規則,凌駕上面部分舊描述。

### 王戰(已大改)
- **傷害 = 總血 %、按題型**(不看字數,免後期爆):`BOSS_DMG = {choice .05, spell .08, two .12, sentence .2, combo .25}`。舊「字母數 + cap 10」已拔。
- **血隨階段縮放**:`bossMaxHp = BOSS_HP_BASE(50) + (stage-1)×BOSS_HP_PER_STAGE(30)`。
- **加權選字**:`bossPickWords` —— 現學批次(`batchOf===stage-1`)權重 `BOSS_CUR_WEIGHT(4)`、舊字 1 → 偏重現學。
- **綜合考 + 句子默寫**:`bossSentenceQuestion`(看中文默寫整句;`pickBuildSentence` 湊不出回 null → 退單字),`BOSS_SENTENCE_PROB(.3)` 機率出。
- **題庫排功能詞**:`bossPool` 加 `pos!=='function'`(膠水在句子練、不在王單獨拼)。
- **臨死反撲**:多題聯合組合技(`bossCombo`/`PANIC_HITS`),全對才打出收尾;答錯逐格歸因 `bossWrongIds`(只記真的拼錯的字 → 惡補不複習已會的)。
- 提示花樣 `BOSS_MODES`(看中/聽/看圖/四選一),`pickBossMode`(沒圖不出 pic、功能詞不出 listen)。

### 功能詞 = 膠水,連教卡都不出,只在句子學(step 3+)
- `buildLevel`: `fresh`/`active`/`due` 都排除 `pos==='function'` → 功能詞**不進關卡**(不出「先認識這個字」教卡、不單獨刷);批次一解鎖(`batchOf<stage`)就**靜默標 `taught`**(讓句子組得出)。意義交給 `teachPattern`(排句子前教結構)+ 句子本身;熟練度靠 `creditSentence`。避免「是 = yes/is/am」歧義 + 「am 只跟 I 搭配卻要你單獨背」。

### 句子練字接通(step 1/2)
- `creditSentence(sentence)`:排對句子 → 拆字對回 BANK、每個組成字 `creditWord`(+mastery + 排 SRS)→「靠排詞自然學會字」真的算數。
- 句子題頻率 phase-aware:`ask` 的 `sentence_build` 權重 stage≥2 = 40(約半數題)、前期 7。
- 念整句走 `speakSentence`(套 `SPEAK_AS`,a→uh 不念字母 A)。

### 補考 = 同題型再考
- `onWrong` 把「剛剛的題型」(`lastAsked[k]`)一起進 `reviewQueue`(存 `{w, run}`);`reviewThenAsk(w, run)` 重答用 `run(w)` → 錯默寫補默寫,不因 mastery 掉而換成簡單認題。

### 新題型:音節克漏字(`askSylType`,寫階長字友善)
- 顯示音節、遮 1~2 節「打」缺的;逐節對錯記 `store[w.id].sylMiss[i]++`;遮哪節加權偏向常錯的(`wt=1+miss×2`);答錯該節有 `SYL_HINT[syl]` 就上記憶法(玩法層 map,台灣向諧音/圖像)。
- ⏳ 待:某節連錯 N 次 → 退回該字一輪聽·看·選;擴 `SYL_HINT`(AI 生使用者審)。

### 語音
- `speak` 挑最佳英文語音(`pickBestVoice`:Google > 微軟 Natural > en-US);設 voice 包 try/catch。句子結算有「🐢 慢速」(rate 0.5)。
